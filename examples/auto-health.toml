# Example with auto_health in TOML
[app]
name = "api-with-auto-health"
version = "1.0.0"

[run]
command = "node server.js"
port = 3000

# No custom health endpoint - Spark will auto-check port 3000
auto_health = true